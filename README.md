# news_list

A news list project written in Flutter that pulls the top 20 Hacker news articles.

- Includes a search filter feature.
- Includes a sort button to sort alphabetically in ascending order.
- Uses riverpods for state management.
- Has a settings page to change the language.